$TTL    86400
@       IN      SOA     ${domain}. root.${domain}. (
                              2         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
;
@       IN      NS      ${domain}.
${a.list}
